package FunctionalProgramming;

public interface Printer {
    void print(String message);
    // void foo(String first, String second); // adding second abstract method makes it non-functional interface
}
